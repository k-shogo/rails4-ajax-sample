<h1>Listing items</h1>

<div id="items">
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody id="items_list">
      <% @items.each do |item| %>
        <%= render 'item', item: item %>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= render 'form' %>
</div>
<%# <%= link_to 'New Item', new_item_path %>
